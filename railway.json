{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd lab && PYTHONPATH=/app:/app/lab gunicorn app.asgi:asgi_app -k uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT"
  }
}
