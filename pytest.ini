[pytest]
testpaths = tests

# tests/frozen/ täcker agentur-lagret (lab/frozen/), fryst 2026-07-12. Testerna raderas inte —
# de är beviset som gör lagret väckbart — men de körs inte, eftersom modulerna de importerar
# medvetet inte längre ligger på scripts-namespacet. Se lab/frozen/FROZEN.md.
addopts = --ignore=tests/frozen
